#!/bin/sh

if [ -z "$NODE_URL" ]
then
      echo "\$NODE_URL is not defined"
      exit 1
fi

set -x
cleos -u $NODE_URL set contract aggregion Aggregion/ -p aggregion@active
cleos -u $NODE_URL set contract dmpusers Dmpusers/ -p dmpusers@active
