#!/bin/sh

ACCOUNT_KEY="$AGGREGION_ACCOUNT@active"

echo NODE: $NODE_URL
echo ACCOUNT: $AGGREGION_ACCOUNT
echo ACCOUNT_KEY: $ACCOUNT_KEY

cleos -u $NODE_URL set contract $AGGREGION_ACCOUNT Aggregion/ -p $ACCOUNT_KEY
